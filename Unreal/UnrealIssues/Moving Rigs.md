#UnrealEngine #TroubleShooting 
Moving a rig from one skeleton onto another means that we will need to reset all zeroes on each control. Otherwise the controls will use the positions of the previous skeleton, destroying the rig.
